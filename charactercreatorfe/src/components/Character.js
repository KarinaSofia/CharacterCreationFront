function Character({Character, deleteCharacter}){


    
}

/*
function ToDoItem({ item, setComplete, deleteItem }) {
 
    return (
        <>
            <div className={item.complete ? 'strike' : ''} onClick={() => { setComplete(item._id, !item.complete); }}>
                {item.title}
                <br />
                {item.description}
            </div>

            {item.complete && <div><button className="delete" onClick={() => { deleteItem(item._id) }}> Remove</button></div>}

        </>
    )
}

export default ToDoItem;
*/